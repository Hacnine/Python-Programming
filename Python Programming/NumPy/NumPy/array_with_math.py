import numpy as np
arr = np.array([1, 3, 5])
add = arr + 2
sub = arr - 5
# print('add:', add)
# print('sub', sub)

arr2 = np.array([5, 8, 3])
add_two_array = arr + arr2
# print(add_two_array)

# cos value

cos = np.cos(arr)
# print('cos value: ', cos)

# max and min value

max_val = np.max(arr)
min_val = np.max(arr)
print(max_val)